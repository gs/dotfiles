# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# This is where you put your own keybinds. Be Mindful to check as well ~/.config/hypr/configs/Keybinds.conf to avoid conflict
# if you think I should replace the Pre-defined Keybinds in ~/.config/hypr/configs/Keybinds.conf , submit an issue or let me know in DC and present me a valid reason as to why, such as conflicting with global shortcuts, etc etc

# See https://wiki.hyprland.org/Configuring/Keywords/ for more settings and variables
# See also Laptops.conf for laptops keybinds 

# /* ---- ✴️ Variables ✴️ ---- */  #
$mainMod = SUPER
$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts
$UserConfigs = $HOME/.config/hypr/UserConfigs

#  IMPORTANT: If you want to remap and existing keybind you MUST unbindd it first 

# The bindings are CASE SENSITIVE. We suggest you copy the exisitng binding here
#  Then change `bindd` to `unbind`

# E.g. 
# unbind = $mainMod, Return, Open terminal, exec, $term
# bindd = $mainMod, Return, Open terminal, exec, ghostty 
#
# unbind = $mainMod, E, file manager, exec, $files
# bindd = $mainMod, T, file manager, exec, $files

# If you are ADDING a bindd, make sure you include the description 
# Other the keybind search menu might not show it properly 

# E.g.
# bindd = $mainMod, Z, My z app, exec APPNAME


unbind = CTRL, W

# Unbind conflicting keybinds from default config
unbind = $mainMod, H, help / cheat sheet, exec, $scriptsDir/KeyHints.sh
unbind = $mainMod SHIFT, K, search keybinds, exec, $scriptsDir/KeyBinds.sh

# Rebind help to Super+Alt+Ctrl+H
bindd = $mainMod ALT CTRL, H, help / cheat sheet, exec, $scriptsDir/KeyHints.sh

# Rebind keybinds search to Super+Alt+Ctrl+K
bindd = $mainMod ALT CTRL, K, search keybinds, exec, $scriptsDir/KeyBinds.sh

# Unbind default monitor movement keybinds
unbind = $mainMod CTRL, F10, move workspace to left monitor, movecurrentworkspacetomonitor, l
unbind = $mainMod CTRL, F10, move workspace to right monitor, movecurrentworkspacetomonitor, r

# Vim-style monitor movement
bindd = $mainMod CTRL, h, move workspace to left monitor, movecurrentworkspacetomonitor, l
bindd = $mainMod CTRL, l, move workspace to right monitor, movecurrentworkspacetomonitor, r

# Vim-style window navigation with hjkl
bindd = $mainMod, h, focus left, movefocus, l
bindd = $mainMod, j, focus down, movefocus, d
bindd = $mainMod, k, focus up, movefocus, u
bindd = $mainMod, l, focus right, movefocus, r

# Vim-style window movement with HJKL (Shift)
bindd = $mainMod SHIFT, H, move window left, movewindow, l
bindd = $mainMod SHIFT, J, move window down, movewindow, d
bindd = $mainMod SHIFT, K, move window up, movewindow, u
bindd = $mainMod SHIFT, L, move window right, movewindow, r

# For passthrough keyboard into a VM
# bind = $mainMod ALT, P, submap, passthru
#submap = passthru
# to unbind
#bind = $mainMod ALT, P, submap, reset
#submap = reset

bind = $mainMod SHIFT, Q, exec, systemctl sleep && hyprlock
bind = $mainMod CTRL, Q, exec, hyprlock

# Apple keyboard backlight controls via Fn+F5/Fn+F6
binde = , F5, exec, $scriptsDir/BrightnessKbd.sh --dec
binde = , F6, exec, $scriptsDir/BrightnessKbd.sh --inc

# Enter "apps" submap when pressing SUPER + A
bind = $mainMod, O, submap, apps

# ---- Apps submap ----
submap = apps

# Chromium shortcuts
bind = , C, exec, chromium-browser https://chatgpt.com && hyprctl dispatch submap reset
bind = , G, exec, chromium-browser https://github.com && hyprctl dispatch submap reset
bind = , M, exec, chromium-browser https://mail.google.com && hyprctl dispatch submap reset
bind = , o, exec, chromium-browser https://ollama.com && hyprctl dispatch submap reset
#bind = , S, exec, pgrep -x "chromium-browser" > /dev/null || chromium-browser https://spotify.com && hyprctl dispatch submit reset
# Exit submap (ESC or SUPER+A again)
bind = , Escape, submap, reset
submap = reset
