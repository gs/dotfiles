hl.config({
  input = {
    -- Polish (programmer's) layout: identical to US QWERTY, with Polish
    -- letters on AltGr (the right Option key). AltGr+a/c/e/l/n/o/s/x/z.
    kb_layout = "pl",
    kb_options = "compose:caps",
    repeat_rate = 60,
    repeat_delay = 250,
    numlock_by_default = true,
    touchpad = {
      scroll_factor = 0.4,
    },
  },
})
