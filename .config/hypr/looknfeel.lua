hl.config({
  general = {
    -- Space between tiled windows. The visible gap is gaps_in * 2 plus the
    -- two borders, so 0 still leaves a 4px seam at border_size = 2.
    gaps_in = 0,
    -- Space between windows and the screen edge (Omarchy default is 10).
    gaps_out = 5,
  },
})
